class Color {
    constructor(){
        this.hexCode
        this.locked = false
    }

    toggleLock(){
        if (!this.locked) {
            this.locked = true
        } else
            this.locked = false
    }

    randomHex(){
        
    }
}